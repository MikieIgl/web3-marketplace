{
  "name": "web3-marketplace-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Web3 Marketplace",
  "main": "index.js",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.ts --network sepolia",
    "seed": "npx hardhat run scripts/seed.ts --network sepolia",
    "export-abi": "node scripts/export-abi.js",
    "test": "npx hardhat test"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.6.1"
  }
}
